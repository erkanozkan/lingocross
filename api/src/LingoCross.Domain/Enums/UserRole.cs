namespace LingoCross.Domain.Enums;

/// <summary>
/// Kullanıcı rolü. Sayısal değerler kalıcı olarak saklandığı için DEĞİŞTİRİLMEMELİDİR.
/// </summary>
public enum UserRole
{
    Teacher = 1,
    Student = 2,
}
